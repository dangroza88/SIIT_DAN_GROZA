// Create a function that takes two numbers as arguments and returns their sum.

function sum(a, b){
    return a + b;
}
console.log(sum(55, 80));

// Write a function that takes an integer minutes and converts it to seconds.

// a = variabila in minute

function conv(a){
    return a * 60;
}
console.log(conv(80));
